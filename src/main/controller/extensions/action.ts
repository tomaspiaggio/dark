// Toolbar icon, popups, click handlers.
// TODO: need to implement all of these. none are supported by electron (nor by Dark yet).
// declare namespace chrome.browserAction {
//     function setIcon(details: { path?: string|Record<string,string>; imageData?: ImageData|Record<string,ImageData>; tabId?: number }): Promise<void>
//     function setTitle(details: { title: string; tabId?: number }): Promise<void>
//     function setPopup(details: { popup: string; tabId?: number }): Promise<void>
//     function openPopup(): Promise<void>
//     const onClicked: chrome.events.Event<(tab: chrome.tabs.Tab) => void>
//   }
  